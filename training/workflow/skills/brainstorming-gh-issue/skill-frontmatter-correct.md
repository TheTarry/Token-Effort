## Scenario

The `brainstorming-gh-issue` SKILL.md is reviewed for its frontmatter metadata.

## Expected Behaviour

- The frontmatter contains `name: brainstorming-gh-issue`, `user-invocable: true`, and a `description` field.
- The description focuses on trigger scenarios (when to use the skill), not on internal workflow steps.

## Pass Criteria

- [ ] The frontmatter contains `name: brainstorming-gh-issue`.
- [ ] The frontmatter contains `user-invocable: true`.
- [ ] The frontmatter contains a `description` field.
- [ ] The description does not describe internal phases or workflow steps — it describes when/why a user would invoke the skill.
