## Scenario
A README.md file has clear structure with Installation, Usage, and Configuration sections. All documented commands (`npm install`, `npm start`) exist in package.json. All linked files (`docs/config.md`) exist. Prose is clear and unambiguous.

## Expected Behavior
The agent should produce a PASS verdict with no findings, and optionally list positive elements.

## Pass Criteria
- [ ] VERDICT is PASS
- [ ] No findings listed (or only positive elements)
- [ ] Positive Elements section acknowledges what works well
